<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Suite config broker (BROKER.md Phase 1): only apps signed with the same suite key may read
         Dragonfly's SuiteConfigProvider. -->
    <permission
        android:name="com.dragonfly.permission.READ_SUITE_CONFIG"
        android:protectionLevel="signature" />

    <!-- Package visibility (API 30+): Dragonfly reads siblings' installed versions and builds
         their launch intents, so each managed package must be declared here. -->
    <queries>
        <package android:name="com.spotter" />
        <package android:name="com.plate" />
        <package android:name="com.cookbook" />
        <package android:name="com.hawksnest" />
        <package android:name="com.magpie" />
    </queries>

    <application
        android:name=".DragonflyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dragonfly">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Dragonfly">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- PackageInstaller session results (incl. the user-confirmation relay). -->
        <receiver
            android:name=".install.InstallResultReceiver"
            android:exported="false" />

        <!-- Suite connection-config broker, readable only by same-signed suite apps. -->
        <provider
            android:name=".suiteconfig.SuiteConfigProvider"
            android:authorities="com.dragonfly.suiteconfig"
            android:exported="true"
            android:readPermission="com.dragonfly.permission.READ_SUITE_CONFIG" />

        <!-- WorkManager on-demand initialization: Hilt supplies the worker factory via
             DragonflyApp's Configuration.Provider, so the default initializer must not run. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>
    </application>

</manifest>
