name: Release

# Automatic release. Any push to main that changes the Android app cuts a signed release APK,
# generates version.json, and publishes a GitHub Release. Dragonfly self-updates through this
# exact release (it lists itself as a managed app), so the same contract it expects from every
# sibling applies to it.
#
# versionCode = epoch minutes: monotonic across workflow changes and always far above any prior
# code, so a fresh release never looks like a downgrade. versionName = major.minor from
# build.gradle + the commit count (readable, monotonic, no tag collision). The jobs check the repo
# out into Dragonfly/ and the sibling Pulse repo into Pulse/ so includeBuild("../../Pulse")
# resolves. Signs with the CI release key when the KEYSTORE_* secrets are set; else the stable key.
on:
  push:
    branches: [main]
    paths:
      - "android/**"
      - ".github/workflows/release.yml"
  workflow_dispatch:

jobs:
  test:
    name: "Android — Unit Tests"
    runs-on: ubuntu-latest
    timeout-minutes: 25
    defaults:
      run:
        working-directory: Dragonfly/android
    steps:
      - uses: actions/checkout@v4
        with:
          path: Dragonfly
      - uses: actions/checkout@v4
        with:
          repository: CDRaab01/Pulse
          path: Pulse
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
      - run: echo "sdk.dir=$ANDROID_HOME" > local.properties
      - run: ./gradlew :app:testDebugUnitTest --no-daemon

  release:
    name: "Android — Release"
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: Dragonfly/android
    steps:
      - uses: actions/checkout@v4
        with:
          path: Dragonfly
          fetch-depth: 0 # full history so `git rev-list --count` gives the commit count
      - uses: actions/checkout@v4
        with:
          repository: CDRaab01/Pulse
          path: Pulse
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
      - uses: gradle/actions/setup-gradle@v4
      - run: echo "sdk.dir=$ANDROID_HOME" > local.properties
      - name: Derive version
        id: version
        run: |
          BASE=$(sed -n 's/.*VERSION_NAME") ?: "\([0-9]*\.[0-9]*\).*/\1/p' app/build.gradle.kts)
          if [ -z "$BASE" ]; then echo "::error::could not read major.minor from build.gradle.kts"; exit 1; fi
          COMMITS=$(git rev-list --count HEAD)
          CODE=$(( $(date +%s) / 60 ))
          echo "name=$BASE.$COMMITS" >> "$GITHUB_OUTPUT"
          echo "code=$CODE" >> "$GITHUB_OUTPUT"
          echo "Releasing $BASE.$COMMITS (versionCode $CODE)"
      - name: Build signed release APK
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
          VERSION_CODE: ${{ steps.version.outputs.code }}
          VERSION_NAME: ${{ steps.version.outputs.name }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > app/keystore.jks
            export KEYSTORE_PATH=keystore.jks
            echo "Signing with the CI release key."
          else
            echo "No KEYSTORE_BASE64 secret; signing with the committed stable key."
          fi
          ./gradlew :app:assembleRelease --no-daemon
      - name: Assert signing identity
        # Guard against a silent key change: if the suite KEYSTORE_* secret ever went missing the
        # build would fall back to the committed stable key, producing an APK that can't install
        # over existing users. Pin the known-good signer cert and fail the release if it differs.
        run: |
          EXPECTED=5a596c9ea21bfaddae572a66514553c0f8c6db4ed796deabe4f56c9040c2cf8a
          APKSIGNER=$(ls "$ANDROID_HOME"/build-tools/*/apksigner | sort -V | tail -1)
          ACTUAL=$("$APKSIGNER" verify --print-certs app/build/outputs/apk/release/app-release.apk \
            | sed -n 's/.*certificate SHA-256 digest: //p' | head -1)
          if [ "$ACTUAL" != "$EXPECTED" ]; then
            echo "::error::Signing key changed (expected $EXPECTED, got $ACTUAL). Refusing to publish — it would be uninstallable over existing installs. If this key change is intentional, update EXPECTED here."
            exit 1
          fi
          echo "Signing identity verified: $ACTUAL"
      - name: Stage APK + version.json for self-update
        run: |
          APK=dragonfly-v${{ steps.version.outputs.name }}.apk
          cp app/build/outputs/apk/release/app-release.apk "$APK"
          SHA256=$(sha256sum "$APK" | cut -d' ' -f1)
          printf '{"versionCode": %d, "versionName": "%s", "sha256": "%s", "minSdk": 26}\n' \
            "${{ steps.version.outputs.code }}" "${{ steps.version.outputs.name }}" "$SHA256" > version.json
          cat version.json
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.name }}
          generate_release_notes: true
          files: |
            Dragonfly/android/dragonfly-v${{ steps.version.outputs.name }}.apk
            Dragonfly/android/version.json
